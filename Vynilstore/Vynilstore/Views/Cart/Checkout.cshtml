@{
    ViewData["Title"] = "Оформление заказа";
}

<div class="container mt-5">
    <h1>Оформление заказа</h1>

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">
            @TempData["ErrorMessage"]
        </div>
    }

    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">Подтверждение заказа</h2>
                </div>
                <div class="card-body">
                    <p>Пожалуйста, проверьте ваш заказ перед подтверждением.</p>
                    <p>Общая сумма заказа: <strong>@ViewBag.CartTotal</strong></p>
                    
                    <form asp-action="PlaceOrder" method="post">
                        @Html.AntiForgeryToken()
                        <div class="form-group mb-3">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="termsCheck" required>
                                <label class="form-check-label" for="termsCheck">
                                    Я согласен с условиями оформления заказа
                                </label>
                            </div>
                        </div>
                        
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="Index" class="btn btn-secondary me-md-2">Вернуться к корзине</a>
                            <button type="submit" class="btn btn-success">Подтвердить заказ</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div> 